import React from 'react'
import { LoginForm } from './Login'

export default function StudentLogin() {
  return <LoginForm title="Student sign in" subtitle="Check eligible drives, apply in one click and track selections." accent="green" showRegisterLink />
}
